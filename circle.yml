machine:
  php:
    version: 5.5.9
dependencies:
  pre:
    - cp .env.example .env
    # - pear install PHP_CodeSniffer
    # - gem install --no-document checkstyle_filter-git saddler saddler-reporter-github

test:
  override:
    # - scripts/saddler.sh
    # - phpunit
    - ./vendor/bin/phpcs -n --standard=PSR2 app resources
    - ./vendor/bin/phpunit
